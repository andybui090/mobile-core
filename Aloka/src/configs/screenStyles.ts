import { StyleSheet } from 'react-native';

export const screenStyles = StyleSheet.create({
  flex1: {
    flex: 1,
  },
  centerWrap: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  flexGrow1: {
    flexGrow: 1,
  },
});
